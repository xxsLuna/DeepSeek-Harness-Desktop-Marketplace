{
  "name": "deepseek-harness-desktop-marketplace",
  "version": "0.0.0",
  "description": "Plugin catalog for DeepSeek Harness Desktop. Not a package; the two devDependencies belong to the CI gate, never to a plugin.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-catalog.mjs",
    "pack": "node scripts/pack-plugin.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "js-yaml": "^4.1.0"
  }
}
